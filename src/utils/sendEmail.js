// utils/sendEmail.js

export function sendEmail(to, subject, body) {
    console.log('Simulated email sending:');
    console.log(`To: ${to}`);
    console.log(`Subject: ${subject}`);
    console.log(`Body: ${body}`);
    // You can later replace this with EmailJS, SMTP, or another real service.
  }
  